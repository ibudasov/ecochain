{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <h1>
        {{ post.title }} — {{ post.datetimeCreated|date("F jS Y") }}
    </h1>
    <p>{{ post.body }}</p>

    <p><a class="btn btn-secondary" href="{{ path('front_blog', {'_format': 'html'})}}" role="button">Go back</a></p>
{% endblock %}